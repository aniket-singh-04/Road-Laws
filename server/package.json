{
  "name": "drivelegal-server",
  "version": "1.0.0",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node --watch dist/server.js",
    "start": "npm run build && node dist/server.js",
    "seed:drivelegal": "npm run build && node dist/scripts/seedDriveLegalCollections.js",
    "test": "npm run build && node --test"
  },
  "author": "",
  "license": "ISC",
  "description": "DriveLegal rule-based traffic law intelligence API",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "mongodb": "^7.2.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
